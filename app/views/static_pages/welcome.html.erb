<div class="container p-3 mx-auto">
  <main class="mt-4 text-center">
    <h1 class="mb-4"><%= @page_title %></h1>
    <p class="lead"><%= @page_description %></p>
    <p class="lead">
      <a href="#" class="btn btn-lg btn-primary mt-2"><%= t("get_started") %></a>
    </p>
  </main>
</div>

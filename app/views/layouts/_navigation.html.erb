<nav class="navbar navbar-expand-lg navbar-light bg-c-color mb-4">
  <div class="container-fluid">
    <%= link_to I18n.t("application_name"), root_path, class: "navbar-brand"  %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to t("pricing"), pricing_path, class: "nav-link" %>
        </li>
        <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to t('my_accounts'), my_accounts_accounts_path, class:"nav-link"  %>
        </li>
        <% end %>
        <% unless user_signed_in? %>
          <li class="nav-item">
            <%= link_to t('login'), new_user_session_path, class:"nav-link"  %>
          </li>
          <li class="nav-item">
            <%= link_to t('signup'), new_user_registration_path, class:"nav-link"  %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
